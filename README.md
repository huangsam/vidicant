# Vidicant

Vidicant analyzes photo and video contents and emits features for AI/ML models.

## Getting started

```bash
# Prepare the environment
cmake -S . -B build

# Build the project
cmake --build build

# Run tests
ctest --test-dir build
```
